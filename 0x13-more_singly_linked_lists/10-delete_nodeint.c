#include "lists.h"

int delete_nodeint_at_index(listint_t **head, unsigned int index)
{
	if (*head != NULL)
	{
		listint_t *temp, *node;
		int count;

		for (count = 0; count < index; count++)
		{
			temp->
		}

		return (1);
	}
	return (-1)
}
