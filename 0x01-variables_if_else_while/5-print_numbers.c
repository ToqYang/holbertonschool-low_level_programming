#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
int num_bdiez;
num_bdiez = 0;
while (num_bdiez < 10)
{
printf("%d", num_bdiez);
num_bdiez++;
}
putchar('\n');
return (0);
}
