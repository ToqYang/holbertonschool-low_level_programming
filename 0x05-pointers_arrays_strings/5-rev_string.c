#include "holberton.h"
#include <stdio.h>
/**
 * rev_string - Print the message of function
 * @s: Pointer of a char
 */

void rev_string(char *s)
{
	int i;
	char a[500];

	for (i = 0; s[i] != '\0'; i++)
	{
		a[i] = *s;
	}

	while (i--)
	{
	}
}
